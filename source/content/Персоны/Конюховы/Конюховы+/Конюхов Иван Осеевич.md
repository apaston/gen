---
{"publish":true,"created":"2025-12-03T23:54:08.000+03:00","modified":"2025-12-03T23:54:08.000+03:00","cssclasses":""}
---


|                        |                                                                                                           |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| !\- | **Конюхов Иван Осеевич / 1885-\- / [[Места/Холмогоры/Балакшинская\|Балакшинская]]** |

**Отец**: [[Персоны/Конюховы/Конюхов Осей Васильевич\|Конюхов Осей Васильевич]]
**Мать**: `= this.Мать`
**Супруг**: Мария Петровна
**Дети**:  Анна-х
**Комментарий**: `=this.Комментарий`

# Источники
```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => p.file.inlinks.includes(dv.current().file.link))
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `${page.file.name} (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```

```dataviewjs
const pages = dv.pages('"Источники"')
  .where(p => {
    const персоны = p["Персоны"];
    if (!персоны) return false;
    return Array.isArray(персоны) 
      ? персоны.some(link => link.path === dv.current().file.path)
      : персоны.path === dv.current().file.path;
  })
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `[[${page.file.name}]] (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```
