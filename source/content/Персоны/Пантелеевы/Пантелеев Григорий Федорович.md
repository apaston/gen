---
{"publish":true,"created":"2025-12-04T00:03:49.000+03:00","modified":"2025-12-04T00:03:49.000+03:00","cssclasses":""}
---


|                        |                                                                                                           |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| ![[Источники/Портреты/Мужской.jpg\|Мужской.jpg]] | **Пантелеев Григорий Федорович / 1856-\- / [[Места/Холмогоры/Сухарево\|Сухарево]]** |

**Отец**: [[Персоны/Пантелеевы/Пантелеев Федор Адрианович\|Пантелеев Федор Адрианович]]
**Мать**: Мария Андреева
**Супруг**: Мария Иванова (1861)
**Дети**:  [[Персоны/Пантелеевы/Пантелеев Михаил Григорьевич\|Пантелеев Михаил Григорьевич]],Яков,Александра,Пелагея,Анна,Дарья
**Комментарий**: `=this.Комментарий`

# Источники
```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => p.file.inlinks.includes(dv.current().file.link))
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `${page.file.name} (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```

```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => {
    const персоны = p["Персоны"];
    if (!персоны) return false;
    return Array.isArray(персоны) 
      ? персоны.some(link => link.path === dv.current().file.path)
      : персоны.path === dv.current().file.path;
  })
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `[[${page.file.name}]] (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```
