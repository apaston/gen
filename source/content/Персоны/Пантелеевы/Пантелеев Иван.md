---
{"publish":true,"created":"2025-12-04T00:03:49.000+03:00","modified":"2025-12-04T00:03:49.000+03:00","cssclasses":""}
---


|                        |                                                                                                           |
| ---------------------- | --------------------------------------------------------------------------------------------------------- |
| ![[Источники/Портреты/Мужской.jpg\|Мужской.jpg]] | **Пантелеев Иван / \--\- / [[Места/Холмогоры/Сухарево\|Сухарево]]** |

**Отец**: `= this.Отец`
**Мать**: Анисья (1749)
**Супруг**: `=this.Супруг`
**Дети**:  [[Персоны/Пантелеевы/Пантелеев Адриан Иванович\|Пантелеев Адриан Иванович]],Никифор,Григорий,Матрона,Дарья,Наталья
**Комментарий**:  Дата смерти: до 1792 - (в 1792 супруга уже вдова), но после 1788 (в 1792 младшей дочери 3 года)

# Источники
```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => p.file.inlinks.includes(dv.current().file.link))
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `${page.file.name} (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```

```dataviewjs
const pages = dv.pages('"Расшифровки"')
  .where(p => {
    const персоны = p["Персоны"];
    if (!персоны) return false;
    return Array.isArray(персоны) 
      ? персоны.some(link => link.path === dv.current().file.path)
      : персоны.path === dv.current().file.path;
  })
  .sort(p => p["Год"], 'asc');

for (let page of pages) {
  dv.header(3, `[[${page.file.name}]] (${page["Год"]})`);
  const content = await dv.io.load(page.file.path);
  dv.paragraph(content);
  dv.paragraph("---");
}

```
